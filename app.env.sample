# Remotely — environment variable reference
# Copy relevant lines to a .env file alongside docker-compose.yaml

# Timezone
TZ=America/New_York

# Hostname
BASE_HOST_NAME=${HOSTNAME}

# Docker bridge gateway (must match the network gateway in docker-compose.yaml)
DOCKER_GATEWAY=172.28.0.1
